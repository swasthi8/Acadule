import { NextResponse } from "next/server";
import { prisma } from "@/lib/prisma";
import { adminGuard, bodyOf, failure, invalid } from "@/lib/admin-api";
export async function GET() { const denied = await adminGuard(); if (denied) return denied; try { return NextResponse.json(await prisma.section.findMany({ include: { class: true, defaultRoom: true }, orderBy: { name: "asc" } })); } catch (error) { return failure(error); } }
export async function POST(request: Request) { const denied = await adminGuard(); if (denied) return denied; const body = await bodyOf(request); const classId = String(body?.classId ?? ""); const name = String(body?.name ?? "").trim(); const capacity = Number(body?.capacity); const defaultRoomId = String(body?.defaultRoomId ?? ""); if (!classId || !name || !Number.isInteger(capacity) || capacity < 1 || !defaultRoomId) return invalid("Class, section name, capacity, and default room are required"); try { const room = await prisma.room.findUnique({ where: { id: defaultRoomId }, select: { id: true } }); if (!room) return invalid("The selected default room does not exist"); return NextResponse.json(await prisma.section.create({ data: { classId, name, capacity, defaultRoomId } }), { status: 201 }); } catch (error) { return failure(error); } }
